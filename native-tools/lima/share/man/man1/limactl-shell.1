.nh
.TH "LIMACTL" "1" "Mar 2026" "Auto generated by spf13/cobra" ""

.SH NAME
limactl-shell - Execute shell in Lima


.SH SYNOPSIS
\fBlimactl shell [flags] INSTANCE [COMMAND...]\fP


.SH DESCRIPTION
Execute shell in Lima

.PP
lima command is provided as an alias for limactl shell $LIMA_INSTANCE. $LIMA_INSTANCE defaults to "default".

.PP
By default, the first 'ssh' executable found in the host's PATH is used to connect to the Lima instance.
A custom ssh alias can be used instead by setting the $SSH environment variable.

.PP
Environment Variables:
  --preserve-env: Propagates host environment variables to the guest instance.
                  Use LIMA_SHELLENV_ALLOW to specify which variables to allow.
                  Use LIMA_SHELLENV_BLOCK to specify which variables to block (extends default blocklist with +).

.PP
Hint: try --debug to show the detailed logs, if it seems hanging (mostly due to some SSH issue).


.SH OPTIONS
\fB-h\fP, \fB--help\fP[=false]
	help for shell

.PP
\fB--preserve-env\fP[=false]
	Propagate environment variables to the shell

.PP
\fB--reconnect\fP[=false]
	Reconnect to the SSH session

.PP
\fB--shell\fP=""
	Shell interpreter, e.g. /bin/bash

.PP
\fB--start\fP[=false]
	Start the instance if it is not already running

.PP
\fB--sync\fP=""
	Copy a host directory to the guest and vice-versa upon exit

.PP
\fB--workdir\fP=""
	Working directory


.SH OPTIONS INHERITED FROM PARENT COMMANDS
\fB--debug\fP[=false]
	Debug mode

.PP
\fB--log-format\fP="text"
	Set the logging format [text, json]

.PP
\fB--log-level\fP=""
	Set the logging level [trace, debug, info, warn, error]

.PP
\fB--tty\fP[=false]
	Enable TUI interactions such as opening an editor. Defaults to true when stdout is a terminal. Set to false for automation.

.PP
\fB-y\fP, \fB--yes\fP[=false]
	Alias of --tty=false


.SH SEE ALSO
\fBlimactl(1)\fP
