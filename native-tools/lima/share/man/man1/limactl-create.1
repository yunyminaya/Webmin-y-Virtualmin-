.nh
.TH "LIMACTL" "1" "Mar 2026" "Auto generated by spf13/cobra" ""

.SH NAME
limactl-create - Create an instance of Lima


.SH SYNOPSIS
\fBlimactl create FILE.yaml|URL [flags]\fP


.SH DESCRIPTION
Create an instance of Lima


.SH OPTIONS
\fB--arch\fP=""
	Machine architecture (x86_64, aarch64, riscv64, armv7l, s390x, ppc64le)

.PP
\fB--containerd\fP=""
	containerd mode (user, system, user+system, none)

.PP
\fB--cpus\fP=0
	Number of CPUs

.PP
\fB--disk\fP=0
	Disk size in GiB

.PP
\fB--dns\fP=[]
	Specify custom DNS (disable host resolver)

.PP
\fB-h\fP, \fB--help\fP[=false]
	help for create

.PP
\fB--list-drivers\fP[=false]
	List available drivers and exit

.PP
\fB--list-templates\fP[=false]
	List available templates and exit

.PP
\fB--memory\fP=0
	Memory in GiB

.PP
\fB--mount\fP=[]
	Directories to mount, suffix ':w' for writable (Do not specify directories that overlap with the existing mounts)

.PP
\fB--mount-inotify\fP[=false]
	Enable inotify for mounts

.PP
\fB--mount-none\fP[=false]
	Remove all mounts

.PP
\fB--mount-only\fP=[]
	Similar to --mount, but overrides the existing mounts

.PP
\fB--mount-type\fP=""
	Mount type (reverse-sshfs, 9p, virtiofs)

.PP
\fB--mount-writable\fP[=false]
	Make all mounts writable

.PP
\fB--name\fP=""
	Override the instance name

.PP
\fB--nested-virt\fP[=false]
	Enable nested virtualization

.PP
\fB--network\fP=[]
	Additional networks, e.g., "vzNAT" or "lima:shared" to assign vmnet IP

.PP
\fB--plain\fP[=false]
	Plain mode. Disables mounts, port forwarding, containerd, etc.

.PP
\fB--port-forward\fP=[]
	Port forwards (host:guest), e.g., '8080:80' or '9090:9090,static=true' for static port-forwards

.PP
\fB--rosetta\fP[=false]
	Enable Rosetta (for vz instances)

.PP
\fB--set\fP=[]
	Modify the template inplace, using yq syntax. Can be passed multiple times.

.PP
\fB--ssh-port\fP=0
	SSH port (0 for random)

.PP
\fB--video\fP[=false]
	Enable video output (has negative performance impact for QEMU)

.PP
\fB--vm-type\fP=""
	Virtual machine type


.SH OPTIONS INHERITED FROM PARENT COMMANDS
\fB--debug\fP[=false]
	Debug mode

.PP
\fB--log-format\fP="text"
	Set the logging format [text, json]

.PP
\fB--log-level\fP=""
	Set the logging level [trace, debug, info, warn, error]

.PP
\fB--tty\fP[=false]
	Enable TUI interactions such as opening an editor. Defaults to true when stdout is a terminal. Set to false for automation.

.PP
\fB-y\fP, \fB--yes\fP[=false]
	Alias of --tty=false


.SH EXAMPLE
.EX

  To create an instance "default" from the default Ubuntu template:
  $ limactl create

  To create an instance "default" from a template "docker":
  $ limactl create --name=default template:docker

  To create an instance "default" with modified parameters:
  $ limactl create --cpus=2 --memory=2

  To create an instance "default" with yq expressions:
  $ limactl create --set='.cpus = 2 | .memory = "2GiB"'

  To see the template list:
  $ limactl create --list-templates

  To create an instance "default" from a local file:
  $ limactl create --name=default /usr/local/share/lima/templates/fedora.yaml

  To create an instance "default" from a remote URL (use carefully, with a trustable source):
  $ limactl create --name=default https://raw.githubusercontent.com/lima-vm/lima/master/templates/alpine.yaml

  To create an instance "local" from a template passed to stdin (--name parameter is required):
  $ cat template.yaml | limactl create --name=local -

.EE


.SH SEE ALSO
\fBlimactl(1)\fP
